export enum EPaymentProvider {
  TELEGRAM_STARS = 'telegram_stars',
  // Добавить другие провайдеры в будущем
}

export enum EPaymentStatus {
  PENDING = 'pending',
  SUCCESS = 'success',
  REFUSED = 'refused',
  CANCELLED = 'cancelled',
}

